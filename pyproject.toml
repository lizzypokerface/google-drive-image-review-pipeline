[project]
name = "google-drive-image-review-pipeline"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "google-api-python-client",
    "google-auth-httplib2",
    "google-auth-oauthlib",
    "tqdm",
    "Pillow",
    "gradio",
]
